<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Admin CMS</title>
        <style>
            html,body{
              width: 100%;
              height: 100%;
              margin: 0;
            }
            *{
              margin-top: 0.7em;
              text-align: center;
            }
            form{
              display: block;
              margin: auto;
              top: 50%;
              transform: translateY(-50%);
              position: relative;
            }
        </style>
        <link rel="stylesheet" href="./static/styles.css">
    </head>
    <body>
        <% if(loggedIn){ %>
            <h1>Admin</h1>
            <form action="" method="post">
                <button class="button-primary" name="logout" type="submit">Logout</button>
            </form>
        <% }else{ %>
            <form action="" method="post">
                <input class="textfield" placeholder="User" name="username" type="text"><br>
                <input class="textfield" placeholder="Password" name="password" type="password"><br>
                <button class="button-primary" name="login" type="submit">Login</button>
            </form>
        <% } %>
    </body>
</html>